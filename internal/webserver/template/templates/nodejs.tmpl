# auraCP-managed Node.js site — nginx reverse-proxies to the per-site
# systemd unit on a loopback port. WebSocket upgrades handled.

{{bot_map}}

server {
    {{http_listen}}
    {{ssl_listen}}

    {{server_name}}
    {{root}}

    {{ssl_certificate}}
    {{ssl_certificate_key}}
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    location ^~ /.well-known/acme-challenge/ {
        alias /var/lib/auracp/acme/;
        default_type "text/plain";
        try_files $uri =404;
    }

    # v0.2.57: optional :80 → :443 redirect (Settings → General toggle).
    {{force_https}}

    {{nginx_access_log}}
    {{nginx_error_log}}

    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Content-Type-Options nosniff always;
    server_tokens off;
    client_max_body_size 64m;

    {{bot_check}}
    {{basic_auth}}
    {{cache_skip}}

    location ~ /\.(?!well-known) { deny all; }

    {{settings}}

    location / {
        proxy_pass http://127.0.0.1:{{app_port}}/;
        {{proxy_cache}}
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 90s;
    }
}
