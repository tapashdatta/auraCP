<script>
  // Thin wrapper around TableScreen — keeps the route binding in App.svelte
  // unchanged while letting the new component live in its own file.
  import TableScreen from './TableScreen.svelte'
</script>

<TableScreen />
